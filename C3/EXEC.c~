#include<stdio.h> 
#include<unistd.h> 
  
int main(int argc, char *argv[]) 
{ 
    int i; 
    //printf(argv);
    printf("I am EXEC.c called by execv() "); 
    printf("\n"); 
    for( i=1;i<argc;i++) 
            printf("\nargv[%d]: %s\n",i,argv[i]); 
    return 0; 
} 
